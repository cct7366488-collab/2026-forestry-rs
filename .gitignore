# Google Drive 系統檔
desktop.ini
*.tmp
~$*
Thumbs.db

# 敏感資料
.env
*.key
credentials.*

# Firebase 本地實際 config（即使列入也安全，但保持與 example 分離）
**/firebase-config.js
**/.firebaserc
**/firebase-debug.log

# Firebase Admin SDK 服務帳戶金鑰（極敏感！可繞過所有 Security Rules）
**/serviceAccountKey.json
**/*-firebase-adminsdk-*.json

# Firebase init 自動裝的 Agent Skills（給 AI agent 的參考文件，會自動更新，不該進 repo）
**/.agents/
**/skills-lock.json

# Claude Code 本地設定（含 permissions、可能記錄 token，絕不上雲）
.claude/

# Node.js
node_modules/
package-lock.json
**/scripts/package.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# GIS 暫存
*.tif.aux.xml
*.lock
.qgz~
*.gpkg-shm
*.gpkg-wal

# 大型資料（視情況解開註解）
# data/raw/
# *.las
# *.laz

# 案件 GIS 原始檔（含承租人姓名/契約書號 PII，按 Cloud.md 規則：資料→Firebase，不上 GitHub）
data/*.geojson
data/*.qmd
data/*.shp
data/*.dbf
data/*.shx
data/*.prj
data/*.cpg

# 編輯器
.vscode/
.idea/
*.swp
.DS_Store

# 土肉桂試驗 — 資料產出（payload/報表/圖/快取）依 Cloud.md 屬資料，正本走 Firebase，不進 GitHub
modules/土肉桂修枝施肥試驗/dry-run/
modules/土肉桂修枝施肥試驗/analysis/output/

# 會議意見原檔（與會者回饋，依 Cloud.md 屬資料、非 GitHub；已歸檔至 5/20 demo 夾，2026-06-11）
reports/2026-05-mid-demo/520教育訓練後之意見.docx
